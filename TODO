-install a serif typeface
-fix not-working firefox
-add mouse handling (for resizing and moving windows)
-think about the shell background color
-think about the keybindings
-when a window is created, move it to free space
	(or at least to $GAP|$GAP).
-when a window is deleted, focus on the next one
