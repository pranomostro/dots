#!/bin/sh

#maybe try out backgrounds by xsetroot

xsetroot -cursor_name left_ptr

setxkbmap lv
xmodmap $HOME/.xmodmap
xset -r 51

bgs -R -C '#fefefe' '/usr/local/img/reward.png'
redshift -b 1.0:0.3 -l 48.1:11.6 &
slstatus &

exec dwm
