#!/bin/sh

#maybe try out backgrounds by xsetroot

xsetroot -cursor_name left_ptr

xmodmap $HOME/.xmodmap

bgs -R -C '#fefefe' '/usr/share/wall/reward.png'
xflux -l 48.1 -g 11.6
slstatus &
batwatch &

exec dwm
