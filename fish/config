if status --is-interactive

umask 066

set -x -g LANG de_DE.UTF-8

set -x -g BUG_PROJECT /home/adrian/proj/begun/spot/core/bugdb
set -x -g EDITOR e
set -x -g VISUAL sam
set -x GPG_TTY (tty)

#apply z
source /usr/local/bin/z

#apply the functions to the current shell
source /home/adrian/dots/fish/functions

#prompts

function fish_greeting
	true
end

function fish_prompt
	set_color 444444

	echo -n (pwd)

	set_color normal

	echo -n ' » '
end

end
