if status --is-interactive

umask 066

set -x -g EDITOR e
set -x -g VISUAL sam
set -x GPG_TTY (tty)
set -x KLONGPATH /usr/local/src/lang/klong/lib

#apply z
source /usr/local/bin/z

#apply the functions to the current shell
source /home/adrian/.config/fish/functions.fish

#prompts

function fish_greeting
	true
end

function fish_prompt
	z --add (pwd)
	set_color 444444

	echo -n (pwd)

	set_color normal

	echo -n ' » '
end

end
