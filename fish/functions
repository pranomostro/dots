#function definition for the fish shell

#fortune-likes, entertaining!

function troll
	command fortune /usr/local/share/troll
end

function bullshit
	command bullshit /usr/local/share/bullshit
end

#having default options to commands which are always useful.

function cl
	command ls --color=always --group-directories-first $argv
end

function ll
	command ls -ali --color=never --group-directories-first $argv
end

function l
	command ls $argv
end

function e
	command ed $argv
end

function nl
	command nl -ba $argv
end

function man
	command man $argv | col -b | less
end

#silence is golden

function bc
	command bc -ql $argv
end

function gdb
	command gdb -ex 'set confirm off' -q $argv
end

function curl
	command curl -s $argv
end

#definition of new commands or just abbreviations

function g
	grep -E $argv
end

function cs
	clear
end

function lc
	wc -l $argv
end

function md
	mkdir $argv
end

function d
	pwd $argv
end

function cd..
	cd ..
end

function tch
	touch $argv
end

function python
	python3 $argv
end

function rm
	r $argv
end

function mcd
	md $argv
	cd $argv
end

function cls
	cd $argv
	ls
end

#rlwrap is great

#repls

function nc
	rlwrap -s 1000000 nc $argv
end

function rc
	rlwrap -s 1000000 rc $argv
end

function ed
	rlwrap -s 1000000 ed $argv
end

function scm
	rlwrap -s 1000000 scm $argv
end

function lua
	rlwrap -s 1000000 lua $argv
end

function kg
	rlwrap -s 1000000 kg $argv
end

function hl
	rlwrap -s 1000000 hl $argv
end

function fth
	if [ (count $argv) -eq 0 ];
		rlwrap -s 1000000 forth -t $FORTHPATH/forth.fth
	else
		rlwrap -s 1000000 forth $FORTHPATH/forth.fth $argv
	end
end

#Just to troll a little bit

function emacs
	ed -r -s
end

function vim
	info Ed
end

function vi
	man ed
end

function nano
	sam $argv
end
